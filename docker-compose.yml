version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: flat_hub_user
      POSTGRES_PASSWORD: flat_hub_password
      POSTGRES_DB: flat_hub_db
    ports:
      - "5432:5432"
    volumes:
      - ./migrations:/docker-entrypoint-initdb.d